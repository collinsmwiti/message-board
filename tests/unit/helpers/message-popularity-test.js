
import { messagePopularity } from 'message-board/helpers/message-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | message popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = messagePopularity([42]);
  assert.ok(result);
});

